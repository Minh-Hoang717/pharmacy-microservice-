package com.pharma.backend.config;

// CORS được cấu hình trong SecurityConfig (security/SecurityConfig.java)
// Không được khai báo CORS ở đây — sẽ xung đột với allowCredentials(true) trong SecurityConfig.
public class WebConfig {
}
